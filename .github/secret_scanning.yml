# Keep the repository's own secret scan free of false positives from the
# intentional (synthetic) credential fixtures used by the test suite.
paths-ignore:
  - 'test/**'
  - 'docs/assets/example-report.json'
